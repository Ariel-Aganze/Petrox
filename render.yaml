services:
  - type: web
    name: petrox
    env: python
    
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn petrox_project.wsgi:application"

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9