services:
  - type: web
    name: petrox
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn config.wsgi:application"
    
    envVars:
      - key: PYTHON_VERSION
        value: 3.11